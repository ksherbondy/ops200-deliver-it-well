version: 2.1
jobs:
  build:
    docker:
      - image: cimg/node:18.11
    steps:
      - checkout
      - run:
          name: Install Dependencies
          command: npm install
      - run:
          name: Run Tests
          command: npm test

workflows:
  build-and-test:
    jobs:
      - build